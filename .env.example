# AWS Configuration
AWS_REGION=us-west-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# DynamoDB Configuration
DYNAMODB_TABLE=teltonika-events

# Device Configuration
DEFAULT_IMEI=862464068525406

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=5000
API_DEBUG=False

# Dashboard Configuration
# If deploying the dashboard separately, set the API URL
API_BASE_URL=http://localhost:5000/api

# Optional: Enable CORS for specific origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: API Authentication (if implementing)
# API_KEY=your_secret_api_key_here

# =======================================================
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with your actual values
# 3. Never commit the .env file to git (it's in .gitignore)
# =======================================================

# Example:
# cp .env.example .env
# nano .env  (or use your favorite editor)
